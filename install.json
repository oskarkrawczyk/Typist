{
  "resources": {
    "body": [
      {
        "type": "style",
        "src": "./style.css"
      },
      {
        "type": "script",
        "src": "./dist/typist.js"
      },
      {
        "type": "script",
        "src": "./dist/split.js"
      },
      {
        "type": "script",
        "src": "./dist/helper.js"
      },
      {
        "type": "script",
        "contents": "TypistInstallHelper.init()"
      }
    ]
  },
  "options": {
    "properties": {
      "location": {
        "title": "Location",
        "type": "string",
        "format": "selector",
        "description": "Select the heading you would like to apply Typist to."
      },
      "variants": {
        "title": "Variants",
        "type": "array",
        "items": {
          "type": "string",
          "title": "Variant",
          "description": "The alternative content you would like to appear in the region selected.  Will replace all the text in that region, but only the text which is different will be dynamically typed."
        }
      }
    }
  }
}
